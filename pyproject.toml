[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "aquapi"
version = "0.1.0"
description = "DS18B20 temperature sensor reader for Raspberry Pi"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
